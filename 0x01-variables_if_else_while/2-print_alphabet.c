#include <stdio.h>

/**
 * main - lowercase alphabets printed
 * new line addedd
 * RETURN: Always 0 (Success)
 */
int main(void)
{
	char ch;
	for (ch = "a"; ch <= "z"; ch++)
	{
		putchar(ch);
	}
	puchar('\n');
	return (0);
}
