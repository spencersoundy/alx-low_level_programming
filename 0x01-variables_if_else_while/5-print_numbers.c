#include <stdio.h>

/**
 * main - -single digit numbers of a base are printed
 * new line created
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
