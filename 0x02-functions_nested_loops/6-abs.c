#include "main.h"

/**
 * _abs - value of a integer is computed
 * @r: r is an integer
 *
 * Return: integer
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (n);
	}
	else if  (n < 0)
	{
		return (-n);
	}
	else 
	{
		return (n);
	}
}
